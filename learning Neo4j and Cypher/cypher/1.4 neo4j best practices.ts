/* 
  --Relational databases can't manage relationships
  --NoSQL can't handle relationship, other problem is that you has duplicate your data again

  --only the graphs can handle the relationships 
*/
